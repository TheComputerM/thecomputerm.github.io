diff --git a/dist/index.js b/dist/index.js
index ef161e9030110abb47dc7f5ea5c44bef37d02bde..24827dc61e5e1e217f00817dc20a0fcf3c111f81 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -705,148 +705,9 @@ __export(fields_exports, {
   multiSelect: () => multiSelect
 });
 
-// src/linker.ts
-var import_ffi_napi = require("@makeomatic/ffi-napi");
-var ref = __toESM(require("@makeomatic/ref-napi"));
-var import_os = require("os");
-var import_path2 = __toESM(require("path"));
-var import_url2 = require("url");
-var import_meta2 = {};
-var dir2;
-if (__dirname) {
-  dir2 = __dirname;
-} else {
-  dir2 = import_path2.default.dirname((0, import_url2.fileURLToPath)(import_meta2.url));
-}
-var myobj = ref.types.void;
-var myobjPtr = ref.refType(myobj);
-var Input = ref.types.void;
-var InputPtr = ref.refType(Input);
-var p = (0, import_os.platform)();
-var dynamicLib;
-if (p === "win32") {
-  dynamicLib = "./binary/huh.dll";
-} else if (p === "linux") {
-  dynamicLib = "./binary/huh.so";
-} else {
-  throw new Error(`Unsupported platform: ${p}. This application only supports Windows and Linux.`);
-}
-var dllfile = import_path2.default.resolve(dir2, dynamicLib);
-var huh = (0, import_ffi_napi.Library)(dllfile, {
-  "CreateInput": ["string", ["int"]],
-  "SetInputOptions": ["int", ["string", "string", "string", "string", "string"]],
-  "FreeStruct": ["int", ["string"]],
-  "RunInput": ["string", ["string"]],
-  "Confirm": ["string", ["string", "string", "string"]],
-  "Select": ["string", ["string", "string"]],
-  "MultiSelect": ["int", ["string", "string", "string"]],
-  "SetTheme": ["void", ["string"]],
-  "Run": ["string", ["string"]],
-  "CreateGroup": ["string", ["string"]],
-  "CreateForm": ["void", ["string"]],
-  "GetValue": ["string", ["string"]],
-  "Spinner": ["void", ["int", "string"]],
-  "Note": ["string", ["string", "string", "string", "int"]]
-});
-function CreateInput(t) {
-  const Ptr = huh.CreateInput(t);
-  return Ptr;
-}
-function SetInputOptions(ptr, opts) {
-  return huh.SetInputOptions(ptr, opts.Title, opts.Description, opts.Placeholder, opts.validators);
-}
-var linker_default = huh;
 
-// src/fields.ts
-var NewInput = class {
-  id;
-  props;
-  type_;
-  /**
-   * 
-   * @param p 
-   * @param type_ 0 for normal inline input 1 for multiline
-   */
-  constructor(p2, type_) {
-    this.id = "";
-    this.props = p2;
-    this.type_ = type_;
-  }
-  /**
-   * load the struct into c memory
-   */
-  load() {
-    let e = CreateInput(this.type_);
-    if (!e) {
-      throw new Error("Error creating struct in Go");
-    }
-    this.id = e;
-    return SetInputOptions(this.id, this.props);
-  }
-  run() {
-    return linker_default.RunInput(this.id);
-  }
-  get value() {
-    return linker_default.GetValue(this.id);
-  }
-};
-var Confirm = (title, affirmative, negative) => {
-  const i = linker_default.Confirm(title, affirmative, negative);
-  return {
-    id: i,
-    run: function() {
-      return linker_default.Run(this.id);
-    },
-    get value() {
-      return linker_default.GetValue(this.id);
-    }
-  };
-};
-var SetTheme = linker_default.SetTheme;
-var Select = (title, options) => {
-  const i = linker_default.Select(title, options.join(","));
-  return {
-    id: i,
-    run: function() {
-      return linker_default.Run(this.id);
-    },
-    get value() {
-      return linker_default.GetValue(this.id);
-    }
-  };
-};
-var multiSelect = (title, options, limit = 0) => {
-  const i = `id_${Date.now()}`;
-  linker_default.MultiSelect(i, title, options.join(","));
-  return {
-    id: i,
-    run: function() {
-      return linker_default.Run(this.id).split(",");
-    },
-    get value() {
-      return linker_default.GetValue(this.id);
-    }
-  };
-};
-var Note = (title, desc, nextlabel, next) => {
-  const i = linker_default.Note(title, desc, nextlabel, next ? 1 : 0);
-  return {
-    id: i,
-    run: function() {
-      return linker_default.Run(this.id);
-    },
-    get value() {
-      return "";
-    }
-  };
-};
-var CreateGroup = linker_default.CreateGroup;
-var CreateForm = linker_default.CreateForm;
-var GetValue = linker_default.GetValue;
-var Spinner = linker_default.Spinner;
 // Annotate the CommonJS export names for ESM import in node:
 0 && (module.exports = {
   Lipgloss,
-  huh,
   initLip
 });
diff --git a/dist/index.mjs b/dist/index.mjs
index 082d2a313b5b393313d1fec69c4ffe79bd247aec..05fd5bbbb70c4ec29601484bf339bcac0fb48f46 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -101,146 +101,7 @@ __export(fields_exports, {
   multiSelect: () => multiSelect
 });
 
-// src/linker.ts
-import { Library } from "@makeomatic/ffi-napi";
-import * as ref from "@makeomatic/ref-napi";
-import { platform } from "os";
-import path2 from "path";
-import { fileURLToPath as fileURLToPath2 } from "url";
-var dir2;
-if (__dirname) {
-  dir2 = __dirname;
-} else {
-  dir2 = path2.dirname(fileURLToPath2(import.meta.url));
-}
-var myobj = ref.types.void;
-var myobjPtr = ref.refType(myobj);
-var Input = ref.types.void;
-var InputPtr = ref.refType(Input);
-var p = platform();
-var dynamicLib;
-if (p === "win32") {
-  dynamicLib = "./binary/huh.dll";
-} else if (p === "linux") {
-  dynamicLib = "./binary/huh.so";
-} else {
-  throw new Error(`Unsupported platform: ${p}. This application only supports Windows and Linux.`);
-}
-var dllfile = path2.resolve(dir2, dynamicLib);
-var huh = Library(dllfile, {
-  "CreateInput": ["string", ["int"]],
-  "SetInputOptions": ["int", ["string", "string", "string", "string", "string"]],
-  "FreeStruct": ["int", ["string"]],
-  "RunInput": ["string", ["string"]],
-  "Confirm": ["string", ["string", "string", "string"]],
-  "Select": ["string", ["string", "string"]],
-  "MultiSelect": ["int", ["string", "string", "string"]],
-  "SetTheme": ["void", ["string"]],
-  "Run": ["string", ["string"]],
-  "CreateGroup": ["string", ["string"]],
-  "CreateForm": ["void", ["string"]],
-  "GetValue": ["string", ["string"]],
-  "Spinner": ["void", ["int", "string"]],
-  "Note": ["string", ["string", "string", "string", "int"]]
-});
-function CreateInput(t) {
-  const Ptr = huh.CreateInput(t);
-  return Ptr;
-}
-function SetInputOptions(ptr, opts) {
-  return huh.SetInputOptions(ptr, opts.Title, opts.Description, opts.Placeholder, opts.validators);
-}
-var linker_default = huh;
-
-// src/fields.ts
-var NewInput = class {
-  id;
-  props;
-  type_;
-  /**
-   * 
-   * @param p 
-   * @param type_ 0 for normal inline input 1 for multiline
-   */
-  constructor(p2, type_) {
-    this.id = "";
-    this.props = p2;
-    this.type_ = type_;
-  }
-  /**
-   * load the struct into c memory
-   */
-  load() {
-    let e = CreateInput(this.type_);
-    if (!e) {
-      throw new Error("Error creating struct in Go");
-    }
-    this.id = e;
-    return SetInputOptions(this.id, this.props);
-  }
-  run() {
-    return linker_default.RunInput(this.id);
-  }
-  get value() {
-    return linker_default.GetValue(this.id);
-  }
-};
-var Confirm = (title, affirmative, negative) => {
-  const i = linker_default.Confirm(title, affirmative, negative);
-  return {
-    id: i,
-    run: function() {
-      return linker_default.Run(this.id);
-    },
-    get value() {
-      return linker_default.GetValue(this.id);
-    }
-  };
-};
-var SetTheme = linker_default.SetTheme;
-var Select = (title, options) => {
-  const i = linker_default.Select(title, options.join(","));
-  return {
-    id: i,
-    run: function() {
-      return linker_default.Run(this.id);
-    },
-    get value() {
-      return linker_default.GetValue(this.id);
-    }
-  };
-};
-var multiSelect = (title, options, limit = 0) => {
-  const i = `id_${Date.now()}`;
-  linker_default.MultiSelect(i, title, options.join(","));
-  return {
-    id: i,
-    run: function() {
-      return linker_default.Run(this.id).split(",");
-    },
-    get value() {
-      return linker_default.GetValue(this.id);
-    }
-  };
-};
-var Note = (title, desc, nextlabel, next) => {
-  const i = linker_default.Note(title, desc, nextlabel, next ? 1 : 0);
-  return {
-    id: i,
-    run: function() {
-      return linker_default.Run(this.id);
-    },
-    get value() {
-      return "";
-    }
-  };
-};
-var CreateGroup = linker_default.CreateGroup;
-var CreateForm = linker_default.CreateForm;
-var GetValue = linker_default.GetValue;
-var Spinner = linker_default.Spinner;
 export {
   Lipgloss,
-  fields_exports as huh,
   initLip
 };
